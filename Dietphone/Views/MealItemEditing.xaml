<UserControl x:Class="Dietphone.Views.MealItemEditing"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:telerikPrimitives="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.Primitives"
    xmlns:telerikInput="clr-namespace:Telerik.Windows.Controls;assembly=Telerik.Windows.Controls.Input"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <UserControl.Resources>
        <Style x:Key="ColorButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="96"/>
            <Setter Property="Height" Value="96"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Margin="{TemplateBinding Padding}">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Unchecked" />
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="border" Storyboard.TargetProperty="BorderThickness">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="6" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FormatButtonStyle" TargetType="ToggleButton">
            <Setter Property="Width" Value="96"/>
            <Setter Property="Height" Value="96"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="BorderBrush" Value="{StaticResource PhoneSubtleBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
        </Style>
        <Style x:Key="HeaderStyle" TargetType="ContentControl">
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Margin" Value="12,0,0,-2"/>
            <Setter Property="Foreground" Value="{StaticResource PhoneSubtleBrush}"/>
        </Style>
        <telerikPrimitives:ApplicationBarInfo x:Key="PickerAppBar" ButtonClick="ApplicationBarInfo_ButtonClick">
            <telerikPrimitives:ApplicationBarButton x:Name="PART_OKButton" Text="OK" IconUri="/Images/DateTimePickerOK.png"/>
            <telerikPrimitives:ApplicationBarButton x:Name="PART_CancelButton" Text="Cancel" IconUri="/Images/DateTimePickerCancel.png"/>
        </telerikPrimitives:ApplicationBarInfo>
    </UserControl.Resources>

    <telerikPrimitives:RadPickerBox x:Name="Picker" Height="0" Width="0" HeaderStyle="{StaticResource HeaderStyle}" ApplicationBarInfo="{StaticResource PickerAppBar}">
        <telerikPrimitives:RadPickerBox.PopupContent>
            <Grid Background="{StaticResource PhoneChromeBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="ILOŚĆ SKŁADNIKA" Foreground="{StaticResource PhoneSubtleBrush}" Margin="24, 26, 24, 26" FontWeight="SemiBold"/>

                <Grid Grid.Row="1" Margin="14, 26, 24, 26">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <TextBox Height="72" InputScope="TelephoneNumber" Text="{Binding Product.ServingSizeValue, Mode=TwoWay}" Grid.Column="0" Grid.Row="0" VerticalAlignment="Top" Margin="0 0 -3 0"/>
                    <telerikInput:RadListPicker Grid.Column="1" Grid.Row="0" Margin="9 3 12 0" VerticalAlignment="Top" SelectedItem="{Binding MealItem.Unit, Mode=TwoWay}" ItemsSource="{Binding AvailableUnitsAndServingSize}" Style="{StaticResource OsListPickerStyle}"/>
                </Grid>


                <!--<telerikPrimitives:RadWrapPanel x:Name="FormatPanel" Grid.Row="1" Margin="14, 26, 24, 26">
                    <ToggleButton x:Name="boldBtn" Style="{StaticResource FormatButtonStyle}">
                        <TextBlock Text="B" FontWeight="Bold"/>
                    </ToggleButton>
                    <ToggleButton x:Name="italicBtn" Style="{StaticResource FormatButtonStyle}">
                        <TextBlock Text="I" FontStyle="Italic" FontFamily="{StaticResource PhoneFontFamilySemiLight}"/>
                    </ToggleButton>
                    <ToggleButton x:Name="underlineBtn" Style="{StaticResource FormatButtonStyle}">
                        <TextBlock Text="U" TextDecorations="Underline" FontFamily="{StaticResource PhoneFontFamilySemiLight}"/>
                    </ToggleButton>
                </telerikPrimitives:RadWrapPanel>-->

                <TextBlock Text="HIGHLIGHT" Foreground="{StaticResource PhoneSubtleBrush}" Grid.Row="2" Margin="24, 26, 24, 26"
                               FontWeight="SemiBold"/>
                <telerikPrimitives:RadWrapPanel x:Name="HighlightPanel" Grid.Row="3" Margin="14, 26, 24, 26">
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Yellow"/>
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Green"/>
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Red"/>
                </telerikPrimitives:RadWrapPanel>

                <TextBlock Text="FONT COLOR" Foreground="{StaticResource PhoneSubtleBrush}" Grid.Row="4" Margin="24, 26, 24, 26"
                               FontWeight="SemiBold"/>
                <telerikPrimitives:RadWrapPanel x:Name="FontColorPanel" Grid.Row="5" Margin="14, 26, 24, 26">
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Orange"/>
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Lime"/>
                    <ToggleButton Style="{StaticResource ColorButtonStyle}" Background="Red"/>
                </telerikPrimitives:RadWrapPanel>
            </Grid>
        </telerikPrimitives:RadPickerBox.PopupContent>
    </telerikPrimitives:RadPickerBox>


</UserControl>
